
  class Car 
    def initialize(make, model, color)
      @make = make
      @model = model
      @color = color
    end

    def make #getter
      @make
    end

    def make=(make) #setter (still confused)
      @make = make
    end

     def model 
      @model
    end

    def model=(model) 
      @model = model
    end

    def color 
      @color
    end

    def color=(color) 
      @color = color
    end
  end
